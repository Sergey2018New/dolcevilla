---

import Icon from '@components/modules/Icon.astro';

interface Props {
    direction?: string;
    ariaLabel?: string;
    class?: string;
    [x: string]: any;
}

const {
	direction,
	ariaLabel,
	class: className,
	...rest
} = Astro.props;
---
<div
    class:list={
        [
            `swiper-button-${direction == 'next' ? 'next' : 'prev'}`,
            className
        ]
    }
    {...rest}
>
    <Icon name="arrow"></Icon>
</div>
